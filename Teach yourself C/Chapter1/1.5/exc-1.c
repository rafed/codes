#include<stdio.h>
int main (void) {

    int l;

    printf("Enter the length of a side of the cube: ");
    scanf("%d", &l);

    printf("The volume of the cube is %d.", l*l*l);

    return 0;
}

//Coded by Rafed.
